import { Translation } from "./translation";
export class Industry {
    _id?: number;
    name: Translation[];
    hasCombinations: boolean;

    created?: Date;
    modified?: Date;
    deleted?: Date;
}